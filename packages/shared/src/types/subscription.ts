/**
 * Defines the possible subscription tiers for users.
 */
export type SubscriptionTier = 'free' | 'standard' | 'premium';

// Add other subscription-related types here if needed in the future
// e.g., interface SubscriptionDetails { ... }